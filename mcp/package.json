{
  "name": "universal-transpiler-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin MCP service boundary for Universal-transpiler",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ajv": "^8.17.1"
  }
}