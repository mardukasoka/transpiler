# MCP Service

Planned thin service boundary over the existing Universal-transpiler.

The initial implementation should call existing translators rather than reimplementing parsers. Project adapters sit above language translation and add asset/runtime/API transformations.

See `docs/MCP_SERVICE.md`.
